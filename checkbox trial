(function (document, $) {
    "use strict";

    // When the dialog gets injected
    $(document).on("foundation-contentloaded", function (e) {
        checkboxShowHideHandler($(".cq-dialog-checkbox-showhide", e.target));
    });

    // On checkbox state change
    $(document).on("change", ".cq-dialog-checkbox-showhide", function () {
        checkboxShowHideHandler($(this));
    });

    // Handle newly added multifield items
    $(document).on("click", ".cq-dialog-multifield-add", function () {
        setTimeout(function () {
            checkboxShowHideHandler($(".cq-dialog-checkbox-showhide"));
        }, 500); // Small delay to ensure the new item is fully rendered
    });

    function checkboxShowHideHandler(el) {
        el.each(function (i, element) {
            const $element = $(element);

            if ($element.is("coral-checkbox")) {
                Coral.commons.ready(element, function (component) {
                    toggleVisibility(component, $element);

                    // Attach event listener for changes
                    component.on("change", function () {
                        toggleVisibility(component, $element);
                    });
                });
            }
        });
    }

    function toggleVisibility(component, $element) {
        // Get the selector for the target field
        const target = $element.data("cqDialogCheckboxShowhideTarget");
        const $target = $element.closest(".coral-Form-fieldwrapper").find(target);

        if (target) {
            if (component.checked) {
                // Show the target field if the checkbox is checked
                $target.removeClass("hide");
            } else {
                // Hide the target field if the checkbox is unchecked
                $target.addClass("hide");
            }
        }
    }
})(document, Granite.$);
